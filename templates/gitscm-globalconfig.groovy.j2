import jenkins.model.*

def inst = Jenkins.getInstance()
def desc = inst.getDescriptor("hudson.plugins.git.GitSCM")

desc.setGlobalConfigName("{{ cj_gitconfig_user }}")
desc.setGlobalConfigEmail("{{ cj_gitconfig_email }}")

desc.save()